export interface UserDetailsDto {
    username : string | null,
    email : string | null,
    dateOfBirth:  string | null,
    address: string | null,
    city: string | null,
    phoneNumber: string
}

